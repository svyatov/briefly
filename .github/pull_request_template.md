<!--
CI already runs RuboCop, `rbs validate`, the tests, 100% line coverage and the YARD stats, so
nothing below asks you to confirm any of that. Delete any heading you have nothing to write under.
An empty section reads as an unanswered one.
-->

## What changed, and why

<!-- One bullet per separable part of the change. Link the issue if there is one. -->

## Public API

<!--
The README's Versioning section lists what Semantic Versioning covers. Name the parts this change
touches, and say for each whether it is additive or incompatible. Write "none" for an internal
change. Under 0.y.z an incompatible change is a MINOR bump, so this is what decides the next version.
-->

## Invariants

<!--
CLAUDE.md's Invariants section lists the properties that make the gem safe, each pinned by a test.
Name any this change interacts with, and the test that still pins it. Write "none touched" if that
is true. If you had to change an invariant, say which and why here rather than in the diff.
-->

## Changelog

<!--
Which bullet under `## Unreleased` in CHANGELOG.md covers this, quoted. That section describes the
net result since the last release, so if this supersedes an existing bullet, edit that one instead
of adding a second. Write "no user-facing change" if none is needed.
-->

## Worth a closer look

<!--
Anything a reviewer would not catch from the diff: a metaprogramming choice with a non-obvious
reason, a case the tests do not reach, a behavior that changed for a caller who was relying on
something undocumented.
-->
